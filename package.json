{
  "name": "hydro-backend",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "build": "babel src -d dist",
    "start": "yarn build && functions-framework --target=latestData",
    "test": "jest",
    "deploy-prep": "yarn install && yarn build && yarn install --production",
    "deploy": "gcloud functions deploy latestData --runtime nodejs10 --trigger-http --allow-unauthenticated --env-vars-file=env.yaml"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/heraldovalenti/hydro-backend.git"
  },
  "author": "Heraldo Valenti <heraldovalenti@gmail.com>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/heraldovalenti/hydro-backend/issues"
  },
  "homepage": "https://github.com/heraldovalenti/hydro-backend#readme",
  "dependencies": {
    "@google-cloud/functions-framework": "^1.7.1",
    "moment": "^2.27.0"
  },
  "devDependencies": {
    "@babel/cli": "^7.10.5",
    "@babel/core": "^7.11.4",
    "@babel/preset-env": "^7.11.0",
    "babel-jest": "^26.3.0",
    "jest": "^26.4.0"
  }
}
